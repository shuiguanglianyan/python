<view class="container">
  <view class="card">
    <view class="title">使用人信息</view>
    <view class="sub-title">用于记录签到操作人</view>

    <view class="field">
      <text class="label">昵称</text>
      <input class="input" value="{{profile.nickname}}" bindinput="onNicknameInput" placeholder="例如：李老师" />
    </view>

    <view class="field">
      <text class="label">手机号</text>
      <input class="input" type="number" value="{{profile.phone}}" bindinput="onPhoneInput" placeholder="选填" />
    </view>

    <button class="primary-btn" bindtap="onSaveProfile">保存资料</button>
  </view>

  <view class="card">
    <view class="title">微信调度 Key</view>
    <view class="sub-title">当前值：{{keyPreview}}</view>
    <view class="tip">你只需修改根目录 config.js 中的 WECHAT_SCHEDULER_KEY。</view>
    <button class="secondary-btn" bindtap="onCopyGuide">复制修改说明</button>
  </view>
</view>
